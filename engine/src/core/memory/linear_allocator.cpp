#include "core/memory/linear_allocator.hpp"

namespace EngineY {

    void* LinearAllocator::New(std::size_t size) {
        return nullptr;
    }

    void LinearAllocator::Delete(void* ptr) {
    }

} // namespace EngineY
